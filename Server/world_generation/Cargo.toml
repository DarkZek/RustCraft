[package]
name = "bevy-testing-world-generation"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
noise = "0.7"
image = { version = "0.24.2", optional = true }

[features]
debug = ['image']